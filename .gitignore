bin/
obj/

# SQLite database is created and seeded fresh on every startup (design.md) — never committed.
*.db
*.db-shm
*.db-wal

.vs/
*.user

#ignore secrets
docs/secrets

# Frontend local config (real API keys/client IDs) — never commit
frontend/src/config.js

# Frontend deps/coverage
frontend/node_modules/
frontend/coverage/