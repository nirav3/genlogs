// Copy this file to `config.js` (gitignored) and fill in real values.
// `config.js` is never committed — see frontend/README.md for setup.

export default {
  GOOGLE_CLIENT_ID: 'REPLACE_WITH_GOOGLE_OAUTH_CLIENT_ID',
  GOOGLE_MAPS_API_KEY: 'REPLACE_WITH_GOOGLE_MAPS_API_KEY',
  API_BASE_URL: 'http://localhost:5136',
};
